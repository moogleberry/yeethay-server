{
  "name": "yeethay-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node ./src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
